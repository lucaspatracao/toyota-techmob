spring.application.name=backend

# Conexão JDBC correta para o Session Pooler do Supabase (porta 5432)
spring.datasource.url=jdbc:postgresql://aws-0-sa-east-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.rdqbrsjfbndcyglrfswm
spring.datasource.password=tech20mob26
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate - Configurações Gerais
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.default_schema=techmob
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Ajuste obrigatório para o Pooler do Supabase no Hibernate 7
spring.jpa.properties.hibernate.boot.allow_jdbc_metadata_access=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Actuator/Health
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always

# Porta do Servidor
server.port=8080